# Internet Gateway: {{ name }}
# Generated by OverCloud

resource "aws_internet_gateway" "{{ id | tf_identifier }}" {
  vpc_id = {{ config.vpc_id | default('aws_vpc.main.id') }}

  tags = {
    Name        = "{{ name }}"
    Environment = var.environment
    ManagedBy   = "OverCloud"
  }
}
